# MongoDB Connection
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster

# JWT Secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_jwt_secret_key_here_min_32_characters

# Server Port
PORT=5000

# Frontend URL (comma-separated for multiple URLs)
FRONTEND_URL=http://localhost:5173

# AI Service (Gemini - Google's API)
GEMINI_API_KEY=your_gemini_api_key_here
# Optional Gemini Model (defaults to gemini-1.5-flash)
# GEMINI_MODEL=gemini-1.5-flash

# Optional: Alternative AI Service (OpenAI)
# OPENAI_API_KEY=your_openai_api_key_here
# Optional OpenAI Model (defaults to gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Node Environment
NODE_ENV=development
